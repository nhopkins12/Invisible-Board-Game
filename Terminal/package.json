{
  "name": "terminal",
  "version": "1.0.0",
  "main": "terminal.js",
  "bin": {
    "myapp": "terminal.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "del \"../public/Terminal.zip\" && npm install && pkg . && rd /S /Q \"./node_modules\" && cd ../ && zip -r Terminal.zip ./Terminal && move ./Terminal.zip ./public/"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@nut-tree-fork/nut-js": "^4.2.2",
    "colors": "^1.4.0",
    "fs": "^0.0.1-security",
    "node-webcam": "^0.8.2",
    "prompt-sync": "^4.2.0",
    "readline": "^1.3.0",
    "socket.io-client": "^4.7.5",
    "socket.io-stream": "^0.9.1"
  }
}
